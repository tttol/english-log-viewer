# English Log Viewer

Minimal Ratatui application scaffold for an English review log viewer.

## Run

```shell
cargo run
```

Press `q` to quit.

The scaffold intentionally contains only an empty feedback table. Log parsing, file watching, and detail views are not implemented.
